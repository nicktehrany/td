#include "td.h"

int add_item(Arguments *args) {
    printf("%d", args->item_length);
    return 0;
}

int delete_item(Arguments *args) {
    return 0;
}

int list_items(Arguments *args) {
    return 0;
}

int finish_item(Arguments *args) {
    return 0;
}
